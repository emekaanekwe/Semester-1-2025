import logging
import os
import random
import sys
import time
import tracebook

class someclass:

    def m1():    
        empty_set = set()
    
        class_var = someclass.explored.copy()
    
        someclass.explored = set()
    
    #getAndRestExplored = staticmethod(getAndRestExplored)
    
    def m2(self, agent_index=0):
        if self.isWin() or self.isLose(): return []
        