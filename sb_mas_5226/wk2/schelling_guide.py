import numpy as np
import random as rd

# initialization
def init():
    agents = [(0,1), (1,0)]
    w = 3
    h = 3
    d = 0.1
    empty = []
    config = np.zeroes(h, w)
    for i in range(w):
        for j in range(h):
            pass
            #rd.random() < d:

                #agents.append()
            
                    
        